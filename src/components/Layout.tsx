import React from 'react';
import { Outlet } from 'react-router-dom';
import Nav from './Nav';

// We need to import Footer. Wait, Footer is currently in App.tsx.
// Let's just define Layout in App.tsx to avoid moving Footer, or I can move Footer to its own file.
